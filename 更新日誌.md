# Beyblade Randomizer — 更新日誌

> 資料來源為 `x` 專案（唯讀）。每次從 x 同步資料/圖片後在此記錄。
> 同步方式：`node sync-from-x.js`（會把 `x/img`、`x/db` 鏡像進 `assets/` 並重打包 `assets/db.bundle.js`）。

---

## 2026-06-12 — x 專案新陀螺同步

**來源**：`x` 專案 `git pull`（落後 21 個 commit）後比對 → 有新增陀螺資料、無新增圖片檔。

### x 端資料變更
- **prod-beys.json 新增 2 筆**（僅編號已確定者；BXG-?? 未定編號者暫留 `-update.json` news）：
  | 產品 | 類別 | abbr | 說明 |
  |---|---|---|---|
  | UX-20 | S(Starter) | `GlVl / LF` | Glory Valkyrie LF；blade `GlVl`✅、bit `LF`✅、ratchet 未公布→`/` 佔位 |
  | BX-50 | RB(Random Booster) | `/ 0-80 DS` | Heaven's Ring 0-80DS；ratchet `0-80`✅、bit `DS`✅、blade 全新未建檔→`/` 佔位 |
- 零件數值修正（隨 pull 帶入）：`part-bit` Narrow `["2=",10]`、`part-blade-divided` Outer `["4-"]`／Whip `["27+"]`。
- `db/-update.json` 的 `prod-beys` 時間戳 bump → `2026/06/12 15:54:00`。

### 同步動作
- `node sync-from-x.js`：db 4 檔（-update / part-bit / part-blade-divided / prod-beys）鏡像進 `assets/db/`，自動重打包 `assets/db.bundle.js`（10 JSON、260 圖、83.0 KB）。
- 驗證：bundle 內含 `UX-20`、`BX-50`；prod-beys 共 **276** 筆。

### 待後續（owner 決定）
- 新零件尚未建檔，prod-beys 以 `/` 佔位顯示空白（不影響 randomizer 運作，graceful fallback）：
  - blade：Heaven's Ring；chip：Drake / Kraken / Hornet / Buck's；assist：Gravity。
- BXG-?? 系列（Drake Brave G4-70I、Kraken Wriggle S3-70O、Hornet Fort R7-60T、Buck's Antlers B2-60D）待官方公布編號後再正式入 prod-beys。
